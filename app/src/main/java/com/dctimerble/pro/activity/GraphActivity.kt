package com.dctimerble.pro.activity

/**
 * Public compatibility entry point retained for existing intents and manifest
 * declarations. The legacy graph screen remains the rendering fallback while
 * statistics calculation is shared through GraphStatsCalculator.
 */
class GraphActivity : GraphActivityLegacy()